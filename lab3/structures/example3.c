/*Typedefs allow us to define types with a different name.
 * We can use the following syntax to remove the struct keyword
 * from each time we want to define a new point:*/

typedef struct {
    int x;
    int y;
} point;
